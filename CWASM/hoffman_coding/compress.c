#include "compress.h"

void compress(char *input, char *output ,CharCode *table, int table_count, int *lookup) {
	unsigned char byte;
	int bit_count;

	for (int i = 0; input[i] != '\0'; ++i) {
		// write in as raw bits
			
	}
}
